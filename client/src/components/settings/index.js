//
import ThemeLocalization from './ThemeLocalization';

// ----------------------------------------------------------------------

export default function ThemeSettings({ children }) {
  return <ThemeLocalization>{children}</ThemeLocalization>;
}
